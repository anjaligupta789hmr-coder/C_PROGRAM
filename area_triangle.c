#include<stdio.h>
int main (){
    float b,h;
    printf("base of triangle:");
    scanf("%f",&b);
    printf("height of triangle:");
    scanf("%f",&h);
    printf("area of triangle = %f",b*1/2*h);
    return 0;
}